.page-header
  %h1
    Manage Forums

= link_to "New forum", new_admin_forum_path, :class => "btn btn-primary pull-right"

%h2 Listing all forums
%table.table.table-striped
  %tr
    %th
      Name
    %th
      Category
    %th
      Actions

  - @forums.each do |forum|
    %tr
      %td
        = link_to forum.name, [:admin, forum]
      %td
        = link_to forum.category.name, [:admin, forum.category]
      %td
        = link_to "Show", admin_forum_path(forum), class: "btn btn-info"
        = link_to "Edit", edit_admin_forum_path(forum), class: "btn btn-warning"
        = link_to "Destroy", admin_forum_path(forum), method: :delete, data: { confirm: 'Are you certain you want to delete this?' }, class: "btn btn-danger"
