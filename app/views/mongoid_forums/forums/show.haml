%section
  = link_to "Add a topic", forum_path(@forum) + '/new', :class => "btn btn-small btn-primary pull-right"
  %br
  %br
  %hr
  = paginate @topics , :theme => 'tb3'

= render partial: 'mongoid_forums/topics/table', :locals => {:topics => @topics}
